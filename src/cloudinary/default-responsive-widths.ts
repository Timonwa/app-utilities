/** The widths a responsive image ladder covers by default — roughly phone, tablet,
 *  laptop, desktop, and large desktop at 1x. */
export const DEFAULT_RESPONSIVE_WIDTHS = [480, 768, 1024, 1280, 1600] as const;
