/**
 * Checks whether a number is negative.
 * @param num - Number to check
 * @returns True if negative
 * @example isNumberNegative(-5) // true
 */
export function isNumberNegative(num: number): boolean {
  return num < 0;
}
