/**
 * Checks whether a number is positive.
 * @param num - Number to check
 * @returns True if positive
 * @example isNumberPositive(5) // true
 */
export function isNumberPositive(num: number): boolean {
  return num > 0;
}
