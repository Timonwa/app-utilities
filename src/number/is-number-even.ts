/**
 * Checks whether a number is even.
 * @param num - Number to check
 * @returns True if even
 * @example isNumberEven(4) // true
 */
export function isNumberEven(num: number): boolean {
  return num % 2 === 0;
}
