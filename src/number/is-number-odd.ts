/**
 * Checks whether a number is odd.
 * @param num - Number to check
 * @returns True if odd
 * @example isNumberOdd(5) // true
 */
export function isNumberOdd(num: number): boolean {
  return num % 2 !== 0;
}
