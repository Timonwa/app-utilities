/**
 * Gets the current time in milliseconds.
 * @returns Unix milliseconds for now
 * @example getCurrentMillis() // 1705276800000
 */
export function getCurrentMillis(): number {
  return Date.now();
}
