/**
 * Now, as a Date — completes the trio with `getCurrentISOString` and
 * `getCurrentMillis`, so code written against this package stays in one vocabulary.
 *
 * @example getCurrentDate() // Date for now
 */
export function getCurrentDate(): Date {
  return new Date();
}
